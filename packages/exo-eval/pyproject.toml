[project]
name = "exo-eval"
version = "0.1.0"
description = "Evaluation and scoring framework for agent outputs"
requires-python = ">=3.11"
dependencies = [
    "exo-core",
]

[tool.uv.sources]
exo-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/exo"]
